<h1>All Posts</h1>
 <% @posts.each do |post| %>
  <% post.id % 5 == 0 ? (post.title = "Spam") : post.title %>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= link_to post.title, post %>
       </h4>
     </div>
   </div>
 <% end %>